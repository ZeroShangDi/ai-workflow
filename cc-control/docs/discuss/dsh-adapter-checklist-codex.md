# DSH 接入：Codex 独立问题清单

> 日期：2026-09-17
> 当前状态：Q1/Q2/Q4、U1～U14 已确认，当前无待用户回答项；Q3 由 U3 收口，R1～R5 审阅完成。下一步为 P0 技术验证，X1～X9 尚未完成实测。U4 页面先占位，U14 采用用户文字约定。
> 本文独立于 `dsh-adapter-checklist.md`（DS 清单），不修改、不替代该清单，不把其中的推荐项当成用户确认。
> 依据：最近两次提交、现有代码审查，以及本次对话中用户的回答。

## 先说明：这份清单需要谁做什么

**用户确定希望得到什么，以及愿意接受什么取舍。AI 负责把它做成，并用证据说明是否做到了。**

例如，“断线恢复后要不要自动继续”是使用行为，可以问用户；“用哪些确认消息防止重复执行”是技术设计，应该由 AI 负责。不能把后一件事包装成选择题，让用户凭阅读方案来担保正确性。

用户主动指定的技术要求同样有效，例如已经选择复用 WS；这不意味着用户要继续决定每个协议字段。AI 若发现既定要求无法同时满足，应先说明具体冲突和影响，再询问需要改变的那一项要求。

### 标记与更新规则

- `【待你确认】`：需要用户决定的行为或范围。未回答之前保持未决；推荐不是默认授权。
- `【待讨论】`：表达方式或协作规则尚未定稿，可以直接批评或改写，不必勾选。
- `【AI 待办】`：由 AI 设计或核查，不占用户的答题清单。
- `【已有回答】`：保留用户已表达的选择，不因原文用了 `[*]` 而要求重答。
- `【已确认】`与`【已验证】`分开：前者表示需求已确定，后者表示技术结论已有证据。
- 单选题写明“选一项”；多选题只用于可以同时成立的需求，并注明数量及冲突关系。事实核查不做成用户选择题。
- 新说明、新问题、答案和状态变化都追加，原题、选项和历史回答保留。每题最后一条状态记录为准；顶部导航可随之更新。
- 明确的文字回答与勾选同样有效。没有回答不等于同意。
- **状态展示规则补充（2026-09-17）**：文首概况、顶部导航和每题的“当前状态”是可更新的索引，回答后必须同步刷新；历史内容继续增量保留。原“未回答”等占位若已失效，明确标为历史，不要求读者翻到文末推断当前进度。

**易读性要求已明确；具体写法仍在试用。** 本稿尝试“实际场景 → 一个需要决定的问题 → 选项及其影响”。是否适合继续采用，见 Q5。

## 当前导航

| 谁处理 | 项目 |
|---|---|
| 已确认 | Q1/Q2/Q4；U1～U14 全部已回答，见各题最新状态记录 |
| 设计审阅完成 | R1～R5；结论已同步至 [设计 spec](dsh-adapter-design-codex.md) |
| 特别范围 | U4：三个业务空页面，后续逐页指导；U8：不新增首次响应指标；U14：按暂停 AWF 后介入的约定，原生 stop 暂不处理 |
| AI 待办 | T1～T8、X1～X9；按 P0 起逐项验证，不要求用户证明技术可行性 |

历史题目、选项和回答保留；每题最新状态为准。确认需求不等于技术已验证。

## 已有回答：不再重复提问

以下记录来源于 DS 清单中的已有回答及本次对话，仅为本稿的约束，不改变原清单状态。

- DSH 接入覆盖全部七个命令：init、plan、run、plugin、server、open、attach。
- 编排提示词归 server，采用模板与平台差异分离的方式。
- 上下文用量放入 host 能力；会话命令注入进入契约；会话管理增加 reset。
- 先整体规划，再分阶段推进。
- 子 Agent 的身份与工具权限不降级。某条实现路线失败，不代表用户同意降级。
- 通信复用现有 WS，不引入 SSE。
- 用户认可“能力验收”的推进方向；具体场景由 AI 整理，尚不能声称已经全部通过。
- 状态转换和通信协议由 AI 设计、核查，不能整份交给用户在脑中实现一遍。

## 需要你决定的使用行为

### Q1 第一次使用 DSH 接入时，你希望自己准备到哪一步？

**当前状态：`【已确认】`　题型：`【单选，选一项】`**

场景：已经安装 DSH，但还没装 AWF 接入插件，DSH 后台服务也没启动。AWF 需要这些条件才能工作。

- [ ] **AWF 自动准备（推荐）**：初始化时安装接入插件，运行任务时按需启动后台服务。首次使用步骤更少，AWF 会写入必要的接入配置。
- [ ] **我手动准备**：AWF 检查缺少什么，并给出操作说明；安装插件和启动服务由我执行。
- [ ] **其他分工**：例如插件自动安装、服务由我启动，请直接说明。

推荐理由：这次目标是完整接入，自动准备能减少每次使用前的额外操作。AI 仍需确保重复初始化不会重复安装，已有配置不会被无关覆盖。

**回答记录：** 选择自动准备，在init时安装插件，plan和run时需要启动后台服务，plan时启动服务后将w-paln...提示词注入项目下新会话，用户直接接着对话就行。run时注意如果plan已经启动了，不要重复启动，这个启动后台服务的操作全局只有一次，和我们的server一样，全局只有一个。

**本轮增补（2026-09-17）｜【已确认】**：init 安装插件；plan/run 确保 DSH 后台可用。DSH 后台服务全局只有一个，plan 已启动时 run 复用，不按项目重复启动。plan 在目标项目下创建新会话并注入规划指令，用户随后在网页继续对话。此处确认的是服务复用；尚不能据此推断 run 必须复用 plan 的对话会话。并发启动、已有服务识别及多项目隔离由 AI 设计验证。

### Q2 执行 `awf plan` 后，你希望在哪里和 AI 讨论计划？

**当前状态：`【已确认】`　题型：`【单选，选一项】`**

场景：你从终端发起规划，接下来需要阅读 AI 的问题、补充需求、确认计划。已有目标确定使用 DSH 网页展示运行过程，但规划时的交互入口还没有明确。

- [*] **进入 DSH 网页继续讨论（推荐）**：规划和后续执行都在同一个界面查看；终端给出对应会话入口。
- [ ] **留在终端讨论**：保持终端内完成规划的习惯，DSH 网页用于查看后续执行。
- [ ] **其他**：请描述希望的操作过程。

推荐理由：与本次采用 DSH 网页作为界面的方向一致。能否顺利定位到会话、怎样识别规划完成或取消，由 AI 解决，不需要你选内部接口。

**历史占位（已失效）：** 未回答。

**本轮增补（2026-09-17）｜【已确认】**：用户选择在 DSH 网页继续讨论，结合 Q1 的文字回答生效；上方“未回答”保留为历史记录，不代表当前状态。

### Q3 连接短暂中断、恢复后，你希望任务怎样继续？

**当前状态：`【恢复范围已由 U3 确认，故障实测待完成】`　题型：`【单选，选一项】`**

场景：AWF 与 DSH 暂时失去联系，但 DSH 可能还在执行。恢复连接后，AWF 已核实任务做到哪一步、哪些指令已经执行。

- [*] **核实清楚后自动继续（推荐）**：短暂断线不需要你回来操作，界面保留断线和恢复记录。
- [ ] **核实清楚后仍先暂停**：展示进度，由你明确点击继续；代价是无人值守任务会停下来等你。
- [ ] **其他**：请说明希望何时自动继续、何时等待。

推荐理由：自动继续更符合已有编排的使用目的。

**两项共有的底线**：如果仍然无法确定某条指令是否执行，AI 的实现应停止自动重发并显示“结果未确认”，不能赌一次重试。怎样核实由 AI 负责；这道题只问核实成功后的操作偏好。

**回答记录：** 其实没太理解，这个断线算是什么？之前cc的server和tmux cc断开？server失败了按理应该走的一直是awf run -r 恢复，恢复之前可能会需要手动重置状态，我理解其实这边应该也是这样。因为基本上不会出现你说的那种断线后恢复的事情，大概率是断了就连不上了。

**本轮增补（2026-09-17）｜【已有回答，原题前提需纠正】**：用户勾选自动继续，同时明确表示场景不清楚，并提出故障后应沿用 `awf run -r` 恢复、必要时先处理状态的理解。因此不把勾选单独解释为同意自动故障恢复。原题混合了“两个进程仍在、仅连接中断”和“AWF server 或 DSH 进程退出”两种情况；CC 的 tmux 控制也不是同一条常驻 WS 链路。AI 应先核查现有恢复行为，再给出 DSH 如何接入它的方案，不先扩展一套恢复机制。是否需要手动重置哪些状态尚待核查，不能记录为已验证事实。T2/T3 的确认、防重复执行和防无限等待仍是 AI 必须解决的正确性问题。

### Q4 这次接入 DSH，要不要同时升级原有 CC 网页的对话展示？

**当前状态：`【已确认】`　题型：`【单选，选一项】`**

场景：目前 CC 网页主要展示终端抓取的文字。把它升级成按消息、工具调用组织的对话展示，需要增加后端能力；DSH 本身已有自己的对话界面。

- [*] **本轮先不扩展 CC 对话展示（推荐）**：保证 CC 现有功能不受影响，完成 DSH 接入；CC 展示升级另行安排。
- [ ] **本轮一起升级**：CC 用户也获得更清晰的对话记录，本轮开发和验收范围随之增加。
- [ ] **其他范围**：请说明这次必须改善的展示问题。

推荐理由：该升级不是 DSH 接入的必要条件。暂缓它不代表删除 CC 网页，也不决定以后是否继续投入。

**历史占位（已失效）：** 未回答。

**本轮增补（2026-09-17）｜【已确认】**：用户选择本轮不扩展 CC 对话展示，保留现有功能；上方“未回答”保留为历史记录。

## 提问方式的试用反馈

### Q5 看完这份清单，你希望怎样改进提问方式？

**当前状态：`【反馈已采纳，评审材料已准备】`　题型：`【多选，可选零至多项，也可直接评论】`**

这是对本稿可读性的反馈，不是项目开工前必须完成的审批。下面几项可以同时选择。

- [ ] 问题再短一些，只保留当前需要决定的事情。
- [ ] 增加具体的操作例子，让我看清选完之后会怎样。
- [ ] 把推荐理由和技术背景移到后面，先让我比较选项。
- [ ] 每次只拿当前阻塞的少量问题来讨论，其余留在文档里。
- [ ] 其他：可以直接指出哪一道题难懂、问错了人，或者其实不需要问。

**反馈记录：** 目前感受良好，不过这是全部问题还是只是部分，看ds的清单让我感觉改动很大，看这个清单又让我想是不是还有什么没确定的。然后确定行为这种方式没问题，技术实现细节我不想确定也没问题，但是架构设计/原则与规范/变更范围与原因/推进步骤以及一些我暂时可能还没想到的问题我是需要确认的。然后举个例子，设计层面比如适配器究竟都需要什么接口，不应该是给我七个英文单词或者其他选项让我来确认够不够，而是将所有需要的能力遍历出来，我来确定到底哪些是需要在适配器环节就打平的。

**本轮增补（2026-09-17）｜【已有反馈，分工修正已明确】**：用户认可按行为提问的易读性，但本稿仅覆盖部分行为与主要审查发现，不是完整设计评审清单。用户还需要确认架构设计、原则规范、变更范围与原因、推进步骤，并希望 AI 主动检查遗漏。AI 应遍历所需能力，解释每项用途及边界，用户据此确认哪些差异应由适配器统一；不能让用户靠几个英文接口名判断是否齐全。具体表达模板仍可继续调整。

## AI 负责解决的问题：不是用户问卷

下面保留审查发现，便于追踪责任。它们需要证据闭合，不需要用户选择实现方案。技术设计的细节应进入 spec；本区只说明为什么要做、怎样知道做成了。

### T1 AI 需要做决策时，能否真正停住后续任务？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：现有代码会在回合结束时进入决策处理，还会在提问工具执行前拦截它。旧方案仅写“改用提交和打断”，尚未说明怎样保留这两种行为。
- **影响**：可能在决策还没完成时派发下一项任务，或让原本需要拦截的提问直接执行。
- **AI 要完成**：获取主会话输出，设计决策期间的等待、结果落盘、恢复及工具拦截；把统一动作与 CC/DSH 各自的输出格式分开。
- **验收证据**：触发决策后没有提前派发；决策结果只记录一次；重复提问、无效结果和重复事件都能收尾。
- **代码依据**：`server/features/decision/handler.cjs`、`server/web/api/hook.cjs`。

### T2 怎样避免断线后重复执行，或把没执行说成成功？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：“指令发出”不等于“DSH 已接收”，已执行的确认也可能丢失。现有 WS 工具没有业务消息上行处理能力。
- **影响**：简单重发可能重复执行，不重发又可能丢任务。
- **AI 要完成**：在已定 WS 约束内设计确认、去重与重连核对，明确返回结果表示什么；决定回传采用何种可工作的机制，不能假定现有 WS 已经支持它。
- **验收证据**：分别在发送前、执行后但确认前断线，以及进程重启时测试；结果能够区分已接收、未接收、无法确认，不盲目重发。
- **代码依据**：`server/web/ws.cjs`；原清单 B1。

### T3 执行中失去连接，任务会不会永远显示“运行中”？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：当前执行器只在派发前检查一次会话；运行中如果一直是 busy，就一直等待。
- **影响**：插件退出后，任务可能永远卡住，单独修改 alive 的返回值不够。
- **AI 要完成**：让失联进入执行器的等待处理，区分短暂断线和持续不可达；恢复后的行为接 Q3，未回答前不冻结自动继续策略。
- **验收证据**：执行中关闭插件或终止 DSH，AWF 能显示失联并结束无限等待；重连不会把旧状态当成当前状态。
- **代码依据**：`server/runtime/executor.cjs`。

### T4 同时使用两个项目时，指令和结果会不会串到另一个项目？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：方案要求按项目选择适配器，却仍以一个固定项目目录配置插件和 MCP。尚未说明多个项目怎样绑定各自的会话和数据。
- **影响**：项目 A 的结果可能写入 B；旧一轮任务的迟到结果也可能覆盖重试后的新一轮。
- **AI 要完成**：选择能保证项目隔离的运行时布局，绑定项目、会话及任务执行批次；旧结果要能被识别。若必须牺牲既有多项目能力，才另提用户取舍题。
- **验收证据**：两个项目交错运行、同名任务、任务重试后旧结果迟到，均不会串账或覆盖新结果。
- **代码依据**：`plugin/core/mcp/awf-state/server.cjs`、`server/run/subagent.cjs`；spec §8。

### T5 换成 DSH 后，上下文清理和任务收尾还能正确工作吗？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：增加 metrics 方法并不会自动替换现有的 CC 用量文件和对话文件读取；reset 与命令注入也还缺完成标准。
- **影响**：AWF 可能读不到实际用量，误判没有产出，或上下文还没清空就继续发任务。
- **AI 要完成**：接通上下文占用和本轮产出的真实数据，区分未知与零；reset 等待实际完成；保留已有命令注入和 reset 的选择。
- **验收证据**：达到清理条件后能完成交接、清空并续跑；清空失败不假报成功；DSH 有新产出时收尾判断能识别。
- **代码依据**：`server/runtime/channel.cjs`、`server/observability/metrics.cjs`。

### T6 启动、停止和打开会话，会不会影响用户其他工作？

**状态：`【AI 待办】`　责任：AI 设计并验证。**

- **发现**：start/stop/reset 三个方法名不足以定义所有命令的行为；原方案还混用了浏览器页面关闭与后台运行时退出。
- **影响**：停一个项目可能误停其他会话；规划入口虽然打开了，却无法识别完成或取消。
- **AI 要完成**：区分 AWF 创建与用户已有的服务、项目会话与共享进程；实现 attach/open 的会话定位及 plan 完成/取消处理。Q1、Q2 决定体验，内部机制由 AI 负责。
- **验收证据**：已有 DSH 服务可正确接入；停止项目不误停无关工作；关闭页面与停止进程分别验证；规划完成和取消都能返回可识别结果。
- **代码依据**：`cli/lib/session.cjs`、`cli/commands/attach.cjs`；原清单 D1、E8。

### T7 怎样证明“接入完成”，而不是仅仅能发出一条消息？

**状态：`【AI 待办】`　责任：AI 编写验收场景并提供结果。**

能力验收的方向已获用户认可。以下是 AI 整理的首版验收范围，不要求用户证明测试设计正确，也尚未声称测试通过：

| 能力 | 应看到的结果 |
|---|---|
| 七个命令 | init、plan、run、plugin、server、open、attach 各自完成其用途；入口和准备行为按 Q1/Q2 落定 |
| 单任务 | 能启动、执行、记录结果，并在 DSH 网页看到过程 |
| 多 Agent | 身份与工具权限真正生效，结果归属正确，没有以普通 agent 冒充专用 worker |
| 决策与人工介入 | 决策未完成不提前推进，停止、回答、继续能闭合 |
| 上下文管理 | 用量可读，交接与清空完成后才继续 |
| 断线与重启 | 不永久等待，不盲目重复执行，不把未知结果显示为成功 |
| 项目隔离 | 两项目互不串会话、指令和结果；旧执行结果不覆盖新执行 |
| CC 回归 | 现有 CC 使用路径仍可工作；新增展示是否纳入由 Q4 决定 |

AI 应将每项拆成可运行的检查，报告通过、失败、未验证及其证据。假件验证形状，真实环境验证链路，不能只凭测试替身通过就宣称 DSH 可用。

### T8 什么时候可以冻结公共契约、开始大范围修改 CC？

**状态：`【AI 待办】`　责任：AI 收敛方案并修订 spec。**

- **发现**：旧 spec 与新清单在 SSE/WS、覆盖范围、降级策略上有冲突；打断、权限等关键能力尚待验证，却计划先改公共契约。
- **AI 要完成**：先验证足以决定契约的关键能力，再确定公共接口；保持“整体规划、分阶段推进”的已有要求。同步处理主链路不用 hooks 却按 hooks 输出设计 shapes 的矛盾。
- **验收证据**：每个未决技术结论有负责人和验证结果；E4 失败不自动降级；spec 不再同时存在相反的“已定”结论。状态转换表、通信协议由 AI 自行设计和验证，不列为用户必答题。
- **补充核查**：原 spike 中状态返回体过大、技能挂载、设置页、一次性任务、插件管理等剩余项也由 AI 留证据闭合，不因未在本次六项主要发现中展开而消失。

## 后续记录

2026-09-17：用户要求先隔离两份清单、评价 Codex 的提问质量。本稿只建立独立清单；DS 原清单保持原样，未替用户勾选任何新增选项，未开始接入实现。


## 本轮增补：设计也要审阅，但不能把实现责任交给用户

**适用范围**：本节补充并修正前面的二分法，以及 T4/T8 中“AI 选择布局/步骤”的表述。AI 负责提出并验证设计；用户审阅架构边界、原则、范围和推进方式。用户确认设计方向不等于为代码或协议的正确性背书。两份清单继续隔离。

### 分工按三层理解

| 层次 | AI 先提供什么 | 用户确认什么 | AI 继续负责什么 |
|---|---|---|---|
| 目标与使用行为 | 场景、选项及实际影响 | 要什么、不做什么、能接受什么 | 检查行为是否实现 |
| 架构与实施安排 | 完整能力清单、职责边界、理由、影响、备选及推进顺序 | 哪些能力统一、哪些差异保留；原则、范围和阶段是否合理 | 证明可行、发现冲突、保证实现符合已定设计 |
| 实现与技术事实 | 代码调查、实验和检查结果 | 不要求逐项选择；用户仍可主动指定约束 | 接口签名、消息格式、算法、测试实现、故障核查 |

### R1 架构与能力边界｜【已审阅；技术验证未完成】

不能用“host 有七个方法”代表能力已遍历。AI 应从七个 CLI 命令、运行编排、决策、上下文管理、观测、插件资产和 UI 的真实调用链梳理能力，再与 DS 清单及原 spec 对照。

能力清单每行至少说明：

| 内容 | 要回答的问题 |
|---|---|
| 能力及用途 | 用户或上层要完成什么？不提供会影响哪个流程？ |
| 当前 CC 与目标 DSH | 两边分别有什么行为和限制？哪些已经证实？ |
| 建议的职责边界及理由 | 适配器统一什么；编排层决定什么；平台保留什么？ |
| 统一后的效果 | 上层是否能不区分平台完成这件事？失败、不可用如何表达？ |
| 用户审阅点 | 该能力是否需要在适配器环节打平？保留差异是否可以接受？ |
| 证据与缺口 | 对应代码、实测与尚未验证项是什么？ |

**表达示例，不是已确认架构**：

| 能力 | 建议边界 | 交给用户审阅的内容 |
|---|---|---|
| 清空对话上下文，并在真正清空后继续 | 适配器提供清空及完成信号；编排决定何时清空、交接哪些内容 | 是否要求所有平台提供这一统一能力，而不是让上层分别发送平台命令 |
| 执行中需要决策时，先拦住后续推进 | 编排判断与记录决策；适配器承担平台所需的观测和拦截动作 | 决策规则是否保持统一、平台差异是否应在适配器内消化 |
| 创建规划对话并让用户接着聊 | 统一“在目标项目创建并打开规划会话”的能力；终端或网页由平台实现 | 是否统一工作流程但允许交互界面不同，而不是强行统一界面形式 |

这三个例子只展示评审材料应长什么样，不能冒充完整能力清单。具体有几个端口、函数叫什么，在职责边界清楚后由 AI 收敛。

### R2 原则与规范｜【已审阅；技术验证未完成】

将已有约束与新增建议分列，说明每条原则解决什么问题、约束哪些模块、如何验证；不能把 AI 新提出的原则悄悄标成已定。

重点包括：编排决策归属、平台差异的收口边界、CC 兼容、全局服务与项目隔离、子 Agent 权限不降级，以及未知执行结果如何处理。实现检查规则由 AI 负责，原则及其代价由用户审阅。

### R3 变更范围与原因｜【已审阅；技术验证未完成】

列出要改哪些模块、为什么非改不可、解决 DSH 接入还是既有债务、会影响哪些现有行为。把“本轮必要”“可独立延后”“明确不做”区分开；不以接入 DSH 为由默认授权所有重构。

Q4 已排除本轮 CC 对话展示升级。公共契约改造仍可能同时影响 CLI、运行时、决策、观测、资产和测试；问题少不代表改动小。

### R4 推进步骤｜【已审阅；技术验证未完成】

AI 提供每阶段的目标、产物、先后理由、验收条件及失败后的处理方式，让用户确认推进安排。关键能力先验证、再冻结公共契约是当前 AI 建议，不能只凭 T8 将具体阶段划分标为用户已确认。已确认的前提仍是“先整体规划，再分阶段推进”。

### R5 清单是否完整｜【已审阅；技术验证未完成】

建立 DS 原清单、原 spec、实际调用链与本稿之间的覆盖对照：每一项标为已有回答、需设计评审、AI 核查、待范围决定或不适用，并说明理由。

目前本稿不是完整清单。T1～T8 记录主要风险，不代表覆盖所有接入能力；AI 应主动查遗漏，不能让用户自己想出所有可能漏掉的问题。完成覆盖对照之前，不以“Q1～Q4 答完”宣称可以进入实现。

### 本轮对技术待办的约束更新

- **T2/T3**：先与现有 `awf run -r` 恢复路径对齐。连接恢复与任务恢复分开设计；没有用户确认，不新增自动从进程故障恢复任务的产品承诺。
- **T4/T6**：Q1 已限定 DSH 后台全局单实例，不能再把“每项目独立启动 DSH 后台”作为自由实现选择。共享运行时下怎样隔离项目由 AI 提出架构方案；若验证发现无法满足，报告冲突，不暗改要求。
- **T7**：验收范围还需与完整能力清单对应，不以现有八行代表所有能力已覆盖。
- **T8**：AI 编写状态转换和通信协议并验证；其中涉及职责边界、既有规范变动和范围扩大时，需进入设计审阅材料。无需用户逐字段审核，也不能因此跳过架构审阅。

2026-09-17（第二轮）：追加 Q1～Q5 的回答解读与分工修正，更新导航；保留用户原始文字、勾选和旧说明。未改动 DS 清单，未开始接入实现。


**归因更正（2026-09-17）**：用户明确指出，“只给几个英文接口名，让人确认够不够”是在评价 DS 清单，不是在评价 Codex 这份清单。用户对 Codex 当前按行为提问的表达方式反馈是“目前感受良好”；对 Codex 的补充要求是说明覆盖是否完整，并纳入架构、原则规范、变更范围与原因、推进步骤的审阅。前文 R1 以该例说明后续材料的写法，不表示 Codex 原清单已经存在同一表达问题。


## 2026-09-18 进展：设计评审材料已准备

材料见 [DSH 接入：设计 spec（Codex）](dsh-adapter-design-codex.md)。

| 原待办 | 已交付的位置 | 当前状态 |
|---|---|---|
| R1 能力与架构 | 设计 spec §2～3：37 项能力、CC/DSH 差异、职责建议 | 材料已准备，待审阅 U1/U2/U5 |
| R2 原则规范 | §6：已有约束与新增建议分列 | 材料已准备，待审阅 U6 |
| R3 范围及原因 | §6：必要模块、原因、明确不纳入范围 | 材料已准备，待审阅 U3/U4/U6 |
| R4 推进步骤 | §7：P0～P4 产物、出口与失败处理 | 材料已准备，待审阅 U7 |
| R5 覆盖检查 | §10：DS 31 题、十三项已定、原 spec 与 T/R 项逐项对应 | 已完成本轮覆盖盘点；新发现仍需追加 |
| Q3 恢复事实 | §5：核对 -r/attach、重复提交、内存 run、遗留 active | 静态核查已完成；建议恢复范围待 U3 审阅，故障实测未完成 |
| T1～T8 技术工作 | §4/§8：源码依据、X1～X8 未知、V01～V12 验收 | 调查已有进展，未完成接入验证，继续保留 AI 待办 |

用户只需审阅能力、边界、原则、范围、步骤和明确的行为取舍；无需替 AI 回答 X1～X8 的技术事实。本轮没有修改 DS 清单、原 spec 或接入代码，也没有安装插件或启动真实模型实验。


**覆盖更新（2026-09-18）**：最终对账发现新增提交 `4b5ed73`。已将 DS A7～A10、A4 的新增选择、D-7～D-13 和能力树纳入设计 spec；当前为 37 项能力（含 1 项待范围确认的首次响应时间）、31 道 DS 问题的对应关系、U1～U8 待审阅项。DS 文件保持原样；两份清单隔离不等于忽略其中已确认的新约束。


## U. 设计待确认问题（从设计稿迁入）

以下是新增的用户评审项，统一在本清单回答和更新状态。文中的章节号均指 [设计 spec](dsh-adapter-design-codex.md)，不是本清单章节。现有 Q1/Q2/Q4 不重问，Q3 已转为 §5 的具体方案。可以直接按编号说修改意见，不必逐格签字。**所有 U 项当前均未确认。**

<a id="u1"></a>

### U1 能力边界是否按 §3 的建议划分？【已确认】

场景：以后增加或更换平台时，希望上层编排继续工作，而不需要到各处判断当前平台。

- [*] 按表中建议：准备、会话、控制、观测及平台动作由适配器统一；调度、门禁、审批、业务落账保持公共（推荐）。
- [ ] 调整部分能力：指出 C 编号及希望归谁处理；其余先按建议。

这里确认职责，不确认端口数量和英文方法名。代价是本轮必须迁移若干已有 CC 泄漏，不能只新增 dsh 目录。

**历史占位（已失效）：** 待确认，尚未作答。

**完整性复核补充**：本题审总体能力边界；能力登记规范另见 U9，状态判定责任另见 U10，不能仅以本题勾选视为两题也已确认。


**状态同步（2026-09-18）｜【已确认】**：接受 spec §3 的能力职责边界；不以端口数量代替能力验收。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u2"></a>

### U2 规划会话和执行会话是否分开？【已确认】

场景：plan 已完成，用户运行 run。共享后台已确定；对话是否沿用还未确定。

- [*] run 新建执行会话，读取已落盘计划；恢复执行时才复用执行现场（推荐）。规划讨论保留供复盘，运行输入更明确。
- [ ] run 沿用规划对话。能保留讨论上下文，但需界定何时接管、上下文容量及用户继续聊天的影响。

未明确的 `plan -r` 建议恢复最近绑定的规划会话；不存在时新建并载入规划状态且说明情况，不静默假称恢复了原对话。

**历史占位（已失效）：** 待确认，尚未作答。

**完整性复核补充**：找不到原规划会话时的行为已独立列为 U13；本题的选择不自动确认原文末尾的 plan -r 建议。


**状态同步（2026-09-18）｜【已确认】**：正常 run 新建执行会话，读取落盘计划；恢复时复用执行现场。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u3"></a>

### U3 是否采用 §5 的最小恢复范围？【已确认】

- [*] 保留 `run -r` 为恢复入口，补齐活跃挂接与现场核对；无法核实的任务明确停下来，暂不建设通用自动崩溃恢复（推荐）。
- [ ] 本轮要求进程崩溃后也自动恢复任务。这会扩大持久化和验证范围，需要另补设计，不能当成 WS 重连的小改动。

第一项会同时修正 CC 当前 -r 的已有缺口；这是一项明确列出的公共行为修复，不隐藏在 DSH 适配内部。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：采用最小恢复范围：run -r 挂接与现场核对；未知结果停下，不建设通用自动崩溃恢复。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u4"></a>

### U4 DSH 网页本轮承接哪些 AWF 页面？【已确认】

场景：原 spec 只写“2～3 个中心页面”，数量不足以说明能完成哪些工作。本稿建议三个业务页面加设置入口，复用 DSH 原生聊天。

- [*] 运行与任务页：进度、任务/WBS、启动/暂停/停止（推荐）。
- [*] 决策与审查页：待处理问题、决定记录、审查结果、动态规划审批（推荐）。
- [*] 记录与诊断页：运行日志、用量、诊断结果及对应会话入口（推荐）。

**多选，可选 1～3 项；未选项表示需要调整本轮 UI 范围，不意味着删除底层能力。** 项目设置与切项目入口作为上述页面的共同配套建议纳入；位置、视觉和每页内部布局由 AI 再设计。已有“暂缓新模式、不做右侧切换条”保持。

**回答记录：** 这些页面到时候我1v1指引，你只需要先加上空页面即可。


**状态同步（2026-09-18）｜【已确认】**：按用户文字回答，先建立所选三个业务页面的空页面；具体页面由用户后续逐页指导，不把勾选解释为现在完成全部页面功能。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u5"></a>

### U5 共享插件安装与项目启用是否分开管理？【已确认】

- [*] 插件代码全局装一次，各项目单独启用/禁用；普通项目禁用不移除其他项目在用的插件（推荐）。AWF server stop 不连带关闭 DSH。
- [ ] 希望所有项目一起启用/禁用，或希望命令同时管理两个后台；请说明期待的范围。

第一项保持项目隔离，但 DSH 的 local/global 与 CC 的物理安装含义不会完全一样，帮助文案需明确行为。

**历史占位（已失效）：** 待确认，尚未作答。

**完整性复核补充**：本题只确认共享安装与项目启用。后台退出和 server stop 的管理边界另见 U12，避免捆绑确认。


**状态同步（2026-09-18）｜【已确认】**：共享插件全局安装，各项目单独启用/禁用。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u6"></a>

### U6 是否接受 §6 的原则和变更边界？【已确认】

- [*] 接受：完成接入所需的公共收口、最小恢复修复、资产和 UI 接线，本轮不做所列无关扩展（推荐）。
- [ ] 调整范围或原则：指出要删、要加或不能改变的部分。

**历史占位（已失效）：** 待确认，尚未作答。

**完整性复核补充**：本题审明确列出的原则和改造范围；不能代替 U9～U14 的独立规范/行为选择。


**状态同步（2026-09-18）｜【已确认】**：接受原则与变更边界；页面和人工介入以 U4/U14 的文字回答为准。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u7"></a>

### U7 是否按 §7 的顺序推进？【已确认】

- [*] 先验证决定架构的关键能力，再收口 CC 契约，再做 DSH 全链路与最终交付（推荐）。
- [ ] 调整阶段或交付优先级：说明先需要看到什么。

U1/U6/U7 是设计审阅，不要求用户保证内部实现可行；AI 仍须负责验证，发现不成立时回报影响并修订。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：按 P0 关键验证、P1 CC 契约收口、P2 DSH 主链路、P3 能力异常路径、P4 交付验证推进；UI 按 U4 分步。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u8"></a>

### U8 首次响应等待时间要不要在本轮新增？【已确认】

场景：最新 DS 能力树提出“提问后首次响应平均时间”；这与已有 Token 用量和输出速度不同，CC 目前也没有该指标。

- [*] 本轮只保留已有用量与输出速度，首次响应时间另行安排（推荐）。接入范围更集中。
- [ ] 两个平台本轮都增加首次响应时间。AI 需定义时间起点、什么算首次响应、平均窗口和精度，并验证采集来源。

这是范围选择，不要求用户设计统计字段；DS 能力树列出这一项尚不等于它已被确认必须实现。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：本轮不新增首次响应时间，保留已有用量和输出速度。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

## 文档分工更新（2026-09-18）

按用户要求，待确认的 U1～U8 已完整迁入本清单，保留原编号和选项，未替用户作答。另一份文件重命名为 `dsh-adapter-design-codex.md`，作为设计 spec；不再保留单独的 review 文件。

- 本清单负责：问题、说明、选项、用户回答、确认状态及 AI 待核查项。
- 设计 spec 负责：能力、架构、原则、变更范围、实施步骤、证据和验收设计；用问题编号关联本清单。
- 用户只需在本清单回答。状态在本清单更新，已确认结论再同步进 spec。


## 2026-09-18 完整性复核：新增问题

复核结论：上一版只有 U1～U8，不能据此宣称用户待确认事项已完整。U9/U10 原来埋在整体架构/原则题里；U11～U14 是没有单独列出的行为边界。以下保持增量追加，不改变已有回答。

<a id="u9"></a>

### U9 是否建立一份可自动检查的适配能力清单？【已确认】【单选】

场景：声明“支持取消”，但真实调用没有接上；这种情况只看文档和方法名很容易漏过。

- [*] 建立能力登记，并在检查中核对真实实现和调用方（推荐）。区分公共能力、平台内部机制；必需能力缺失就明确失败，可选能力不支持也要明说。会增加登记维护与检查工作，但能持续发现“写了却没接上”。
- [ ] 只维护接口与行为测试，不增加独立登记和自动对账。维护内容更少，但文档与实际接线是否一致主要靠评审发现。
- [ ] 其他：说明希望保留哪些检查、避免哪些维护负担。

来源：DS A9/F2；设计 spec §3/§6。这里确认管理规范，登记字段和检查实现由 AI 负责。推荐少量稳定的能力分组，不把“端口必须 ≤8”作为本轮硬性规则。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：建立能力登记并自动检查实现和调用方，不将端口数量上限作为硬规则。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u10"></a>

### U10 “能否继续派发任务”是否统一由 AWF 判定？【已确认】【单选】

场景：DSH 已经结束一轮回答，但 AWF 还在处理决策或等待人工批准。此时平台虽然空闲，工作流还不能继续。

- [*] 平台报告运行事实，AWF 综合暂停、决策、任务状态判断能否继续（推荐）。连接初始和恢复时核对平台现状，运行时接收事件更新；CC/DSH 共用一套派发判据。
- [ ] 每个平台适配器都对外给出最终“可以继续”状态。这会把部分工作流条件放进适配器，需要说明怎样避免两边规则不一致。
- [ ] 其他：说明期望由哪层掌握最终推进判断。

来源：DS A7/A10；C09/C12/C21。D-12 已确认需要回调能力，此题不重问要不要回调，也不让你决定靠轮询还是推送实现。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：平台报告事实，由 AWF 综合工作流状态决定能否继续派发。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u11"></a>

### U11 “打断这次回答”和“停止整个 run”要不要分开？【已确认】【单选】

场景：主会话正在回复，同时有子 Agent 执行，队列里还有下一条指令。只打断主会话，其他工作可能继续。

- [*] 分成两个明确动作（推荐）：“打断回答”只针对选定会话；“停止 run”停止后续派发，并取消该 run 的主会话工作、子 Agent 和待执行指令。其他项目和历史对话保留。
- [ ] AWF 只提供“停止 run”一个控制入口。当前回答、子 Agent 与待执行指令一起停止，减少控制项，但不能从 AWF 单独打断一个回答。
- [ ] 其他：描述希望停止哪些工作、保留哪些工作。

来源：C13/C14、DSH cancel 保留待执行队列的源码事实；当前 AWF `/stop` 只是打断主会话，不能直接宣称已具有完整停止 run 的语义。DSH 原生按钮怎样与 AWF 动作区分、如何验证父子任务停止，由 AI 负责；不是通过改按钮文字就算完成。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：区分打断选定回答与停止整个 run；后者覆盖所属主会话工作、子 Agent 与待执行指令。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u12"></a>

### U12 最后一个 AWF 任务结束后，DSH 后台是否继续保留？【已确认】【单选】

场景：多个项目共享一个 DSH 后台，当前所有 AWF 任务都结束了。全局只启动一个后台已经确定，但什么时候退出尚未确定。

- [*] 保留后台，供网页和下一次 plan/run 继续使用（推荐）。AWF 不因为任务结束自动关闭 DSH；`awf server stop` 仍只管理 AWF server。
- [ ] 允许空闲回收，但仅限 AWF 自己启动、并确认没有其他 DSH 会话在使用的后台。能节省资源，但需要可靠识别所有使用者；无法确认空闲时不能关闭。
- [ ] 其他：说明希望由哪个操作触发关闭。

本题从 U5 的共享安装问题拆出，不重问全局单实例。无论选择哪项，退出项目不能误停其他项目或用户的独立 DSH 工作。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：任务结束保留 DSH 后台；awf server stop 只管理 AWF server。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u13"></a>

### U13 `plan -r` 找不到上次规划对话时，应该怎么办？【已确认】【单选】

场景：规划状态还在项目里，但原对话已删除或无法恢复。能保留计划文件，不等于还能恢复全部讨论上下文。

- [*] 明确说明原对话不可恢复，新建会话并加载已有规划材料（推荐）。可以继续规划，但不会声称保留了丢失的聊天内容。
- [ ] 停下来报告原因，由我决定是否新建会话。不会意外开启新讨论，但需要额外操作。
- [ ] 其他：说明期望的恢复方式。

来源：C07；此前 U2 末尾只有建议，没有独立确认。U2 只决定正常 plan 与 run 的会话关系，不替代本题。

**历史占位（已失效）：** 待确认，尚未作答。


**状态同步（2026-09-18）｜【已确认】**：原规划对话无法恢复时明确说明，新建会话加载已有规划材料。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

<a id="u14"></a>

### U14 run 执行中，你直接在 DSH 对话框发消息时，希望 AWF 怎么配合？【已确认】【单选】

场景：AWF 正在派发任务，你在同一执行会话补充要求。手动消息与自动派发可能同时进入会话；现有 AWF 的介入入口要求先暂停，而 DSH 原生输入框未证明会遵守这一约束。

- [ ] 执行会话先进入人工介入状态，再接受手动补充；AWF 暂停新增派发，完成介入后由我明确继续（推荐）。减少人工指令和调度竞争。暂停派发不等于已经打断所有子 Agent，后者由 U11 的动作决定。
- [ ] 保持原生自由输入，AWF 与人工都可向该会话提交。操作更自由，但 AI 必须另行设计冲突处理，不能继续假定只有 AWF 在驱动。
- [ ] 其他：例如只允许在独立会话讨论，不直接接管执行会话。

来源：C12/C22/C35，当前 `server/web/api/session.cjs` 的 send/intervene 行为。是否能在提交前接管原生输入需要 X9 验证；不能只在消息发出后暂停就宣称做到了第一项。普通规划会话不受此运行期限制。

**回答记录：** run 执行中如果模型正在执行那么会话输入框本身就不能发送，输入框上有暂停awf按钮，手动点击并且模型响应结束后方可手动介入，这为约定。用户直接在输入框中点击sotp停止当前会话属于非正常操作，暂不做处理。


**状态同步（2026-09-18）｜【已确认】**：按用户文字约定：执行时输入不可发送；输入框提供暂停 AWF 按钮，手动暂停且模型响应结束后才可介入。直接点击原生 stop 属非正常操作，本轮暂不处理；实际 UI 行为由 X9 验证。 来源：本题用户勾选或文字回答；未完成的实测仍由 AI 负责。

## 问题总账（2026-09-18，本轮复核）

**统计的是不同责任，不是把数字相加当成用户欠答题数。** 当前新增设计/行为题为 U1～U14，共 14 项；Q1/Q2/Q4 已确认，Q3 的后续取舍归 U3，Q5 的反馈已采纳。AI 技术项为 T1～T8，其具体核查项 X1～X9 如下。R1～R5 是材料产出，不额外要求再签一遍确认。

### DS 31 题逐项处置

“已有回答”表示不再重复问；“待用户”明确指向本清单问题；“AI 负责”并不表示已验证。

| DS 题 | 处置 | 本清单的确认/核查入口 |
|---|---|---|
| A1 | 已有方法选择，补能力边界审阅 | 已有回答；U1 |
| A2 | 已回答：命令注入进契约 | 已有回答；T5 |
| A3 | 已回答：增加 reset | 已有回答；T5/T6 |
| A4 | 已回答：参数化值 + extract，不新增 workers 端口 | 已有回答；T1/T7 |
| A5 | AI 按能力盘点生成平台参数 | T8；不要求用户确定字段数量 |
| A6 | 回调返回值已归 D-12，AI 分离平台编码 | T1；职责边界 U1 |
| A7 | 回调能力已定，生产接线由 AI 补齐 | T1；推进状态职责 U10 |
| A8 | 直连/回退必须适配，避免隐含删除 CC 能力 | T6/T8；变更边界 U6 |
| A9 | 待用户确认能力管理规范 | U9；不再仅埋在 U1/U6 |
| A10 | 待用户确认状态职责 | U10；机制核查 T1/T3 |
| B1 | WS 已定，可靠性实现由 AI 负责 | T2；恢复范围 U3 |
| B2 | 已回答：不升级 CC 对话展示 | Q4 |
| C1 | AI 验证命令命名和调用 | X5/X8 |
| C2 | AI 验证技能单源挂载 | X8；资产范围 U6 |
| C3 | 不降级已定，AI 选择等价实现 | X1/X2 |
| C4 | AI 归一事件；主链路不用兼容 hooks | T1/T2；状态职责 U10 |
| D1 | 已回答：自动启动、全局单实例 | Q1；退出策略 U12 |
| D2 | 自动安装已答；共享包与项目启用待审 | Q1；U5 |
| D3 | AI 设计配置生成与作用域 | X1/X8 |
| E1 | AI 核查打断 | X4；用户控制范围 U11 |
| E2 | AI 实测文本/命令区别 | X5/X8 |
| E3 | AI 实测工具权限 | X1/X2 |
| E4 | AI 实测专用 worker 实例，不自动降级 | X2 |
| E5 | AI 核查输出结构与缺失情况 | X5 |
| E6 | AI 选择隔离短调用实现 | X7；不静默削弱可见性和取消能力 |
| E7 | AI 实测插件管理 | X8 |
| E8 | 已回答网页规划，具体会话关系待审 | Q2；U2/U13 |
| F1 | AI 设计并执行分层验证 | T7 |
| F2 | 维护规范用户审，检查实现 AI 负责 | U9；T7 |
| G1 | 文档分工已由用户明确 | 本清单 + design spec；T8 |
| G2 | 先整体规划已定；阶段安排待审 | U7 |

### DS 十三项已定决策

| 决策 | 对应内容 | 去向 |
|---|---|---|
| D-1 | 七命令覆盖 | 已有回答；U6 审变更边界，不撤销覆盖 |
| D-2 | 编排提示词归 server | 已有回答；T8 |
| D-3 | metrics 归 host | 已有回答；T5 |
| D-4 | 先整体规划再分阶段 | 已有回答；U7 |
| D-5 | worker 不降级 | 已有回答；X2 |
| D-6 | WS | 已有回答；T2 |
| D-7 | host 方法集 | A1/A2 已有回答；U1 审语义边界 |
| D-8 | session 方法集 | A3 已有回答；T5/T6 |
| D-9 | CLI 安装能力与 CC 同步 | 已有约束；当前仅检查/指引，不扩大为代装 |
| D-10 | 辅助工具与 CC 同步 | 已有约束；X8 验证依赖检查 |
| D-11 | DSH attach/dashboard 均打开页面 | 已有回答；U4 审业务页面范围 |
| D-12 | 回调能力与返回值 | 已有回答；U10 只审状态责任，X3 验证拦截 |
| D-13 | oneshot 属能力面 | 已有回答；X7 |

### 37 项能力的确认与核查归属

以下覆盖设计 spec 的全部 C 编号；一题可以审一组相关边界，但独立行为已拆题。能力表不是 37 道额外用户题。

| 能力编号 | 确认依据或待审问题 | AI 核查 |
|---|---|---|
| C01,C02 | U1/U6；按项目配置、缺省 CC 为已有 spec 方向 | T8/X8：解析、依赖、运行中不热切换；不额外提安装器需求 |
| C03,C04 | Q1、U5 | X1/X8 |
| C05 | Q1、U12 | T6/X8 |
| C06,C07 | Q1/Q2、U2/U13 | T6/X5 |
| C08 | D-11、U4 | X6 |
| C09,C10,C11,C12 | 已有 host 能力、U1/U10 | T1/T2/X5 |
| C13,C14 | U11/U12 | X4 |
| C15 | 已有 reset、U1 | T5/X4 |
| C16 | Q3、U3 | T2/T3 |
| C17,C18,C19,C20 | A4、D-5、U1 | X1/X2/X5 |
| C21,C22 | D-12、U1/U10/U14 | X3/X9 |
| C23,C24 | D-13、U1/U6 | X7 |
| C25,C26,C27,C28 | 已有 snapshot/metrics、U1/U6；保留数据精度差异 | T5/X6 |
| C29,C30 | D-2、D-5、U1/U6 | X1/X2/X8 |
| C31,C32 | Q2/Q4、U4/U5 | X6 |
| C33 | Q3、U3 | T3 |
| C34,C35,C36 | U1/U6/U10/U14 | T1/T2/T7/X9 |
| C37 | U8 | 范围确认后由 T5 定采集和统计 |

### 技术核查子项：无需用户作答

| 编号 | 问题 | 当前状态及所属待办 |
|---|---|---|
| X1 | 单后台下按项目/会话挂 MCP，子 Agent 继承是否正确 | 待实际验证；T4 |
| X2 | 会话级 MCP 与 worker 权限组合、专用实例是否成立 | 待实际验证；T1/T7 |
| X3 | 主会话输出、提问前拦截和决策顺序 | 待实际验证；T1 |
| X4 | cancel/队列/父子停止/reset 完成信号 | 已有源码证据，链路待验；T5/T6 |
| X5 | 内部服务、事件作用域、输出解析、续话 | 已有源码证据，链路待验；T1/T2 |
| X6 | 用量/进度/历史/设置页/鉴权 UI 通路 | 部分历史 spike 与源码证据，组合待验；T5/T6 |
| X7 | 一次性任务的超时、取消、隔离和可见性 | 待实际验证；T6 |
| X8 | 安装/卸载/技能命令/发布包/状态返回大小 | 已有局部源码证据，交付链路待验；T6/T8 |
| X9 | 原生输入能否在提交前协调人工与 AWF 控制，关闭/切换页面是否影响 | **部分已验证**：关闭页面不影响后台运行（页面不在时任务仍 running）；U14 三条交互约定待用户实机确认；新增阻塞 F34（子会话 bash 停在 approval/asked） |

### spec 中其他建议为什么没有再各问一题

| 建议或边界 | 处置理由 |
|---|---|
| 命令 ID、确认字段、POST 回传、连接校验细节 | T2 负责；U1 审总体职责，WS 已定，不让用户再设计消息协议 |
| 项目 MCP 作用域 | U1/U5 约束范围，X1/X2 验证机制；不能用户一勾就算成立 |
| 最小执行归属持久记录 | U3/U6 明示的恢复范围；记录字段由 AI 设计 |
| DSH 保存原生对话、AWF 保存业务事件与引用 | 原 spec §8.5 已有方向，U1/U6 可审；不新增完整对话库 |
| CC 离线回退 | 保留已有能力、移入 CC 内部是兼容约束；若实际做不到才提能力变更，不默认删掉 |
| 插件设置入口、空白页入口及按钮位置 | 共同功能已在 U4 明示；具体布局由 AI 设计后展示，不因位置未定虚构一个架构阻塞 |
| 不兼容版本、缺能力、无法核实结果 | 明确失败是 U6 的设计原则；AI 负责检测，不自动安装升级或重置业务状态 |
| 不新增文件系统强隔离 | 本轮只承诺已有工具权限不降级；强隔离是另一项未被请求的扩展，不把它当待答题 |

**结论边界**：目前列明的来源（DS 31 题、D-1～D-13、Codex Q/T/R、spec 的 37 项能力和行为建议）均有去向。此前的八题不足，现已补到 U1～U14；这表示本轮已知问题已逐项登记，不表示未来实现不可能发现新问题，更不表示任何未选项已经确认。


## 2026-09-18 回答同步与开工前核对

U1～U14 均已有明确回答：U4/U14 以文字回答限定范围，其余按用户勾选生效。当前无新增待用户确认事项。此前总账中的“待用户”“待审”和正文中的旧占位为历史状态，已由本轮确认覆盖；不删除历史选项和说明。

R1～R5 的设计方向审阅完成，不代表 T1～T8 或 X1～X9 验证通过。下一步按已确认 U7 进入 P0；发现既定要求无法同时满足时，先提供实验依据和影响，再将具体冲突追加到本清单。

U4 的空页面不等于完成业务 UI：后续逐页设计仍需用户指导，但不阻塞 P0 和后台主链路。U14 不额外承诺拦截所有原生输入或处理原生 stop；需验证约定的暂停按钮、等待响应结束和输入状态。


## 2026-09-18 P0 实测发现的冲突：U15（**新增，未确认**）

> 本节按清单规则**追加**，不改动任何历史回答。来源：P0 的 E-03/E-04 实测（见 [执行记录](dsh-adapter-execution.md)）。

### U15 X2「worker 权限不降级」与「项目隔离」当前不能同时满足，需要你选一项【待你确认】【单选】

**实测事实（两组对照，均为真实运行 + 真实模型调用）**

| 实验 | MCP 挂载位置 | 不过滤的 worker | `deny:[该 MCP 工具]` 的 worker | 第二个 worker（`deny:['bash']`） |
|---|---|---|---|---|
| A | **全局层** | 调用成功 → `0.2.0` | **被拒绝（回 `DENIED`）** ✅ 硬拦生效 | 调用成功 → `0.2.0`（互不影响） |
| B | **会话作用域**（项目隔离所需） | 子 Agent **看不到**该工具 | `subagents.start` 直接抛错：`tools.restrict() names unknown global tool …` | — |

- DSH 的 `tools.restrict()` 只能点名**全局层**的工具（`dsh-tools` 源码 + 实测报错一致）。
- 而「每个项目用各自的 `AWF_PROJECT_ROOT`」要求**按会话**挂 MCP（E-03 已证明这条能通，两项目互不串）。
- 两者相交后：**按会话挂载 ⇒ worker 拿不到工具级硬限制；全局挂载 ⇒ 所有会话共用一个固定 env 的 MCP，项目会串。**

**请选一项：**

- [ ] **保项目隔离（我倾向）**：MCP 按会话挂载；worker 的工具限制**本轮达不到 CC 那种硬拦**。
      需要你接受：worker 对 AWF MCP 工具的约束只能靠 worker 身份/提示词协议（与 CC 现状同类，不是新增降级），
      而 DSH 原生的 `tools.restrict()` 在**非 MCP 工具**（bash/read/write 等）上仍然可用、可硬拦。
- [ ] **保工具级硬拦**：MCP 改为**全局挂载一次**（每项目不同 `serverName`，或单实例 + 调用内指定项目）。
      需要你接受：项目隔离不再由「会话作用域」保证，而要另设计一套「按调用指定项目」的机制（会改 AWF MCP 工具签名），
      并且你必须确认多项目同时运行时不会误改另一个项目的 state。
- [ ] **其他**：说明你更看重哪一边，或允许我继续找第三条路（例如验证子 Agent 能否继承父会话的作用域注册）。

**影响范围**：T-P0-07（X2）结论、C17～C20 的实现形态、spec §3 的能力边界表、以及「子 Agent 身份与工具权限不降级」这条已有要求的落地方式。

**在得到回答前**：E-04 保持 blocked；P0 中不依赖它的任务（能力登记、决策拦截、cancel/reset、恢复与失联、安装包、UI 入口）继续推进，不等。

**状态同步（2026-09-18）｜【已确认】**：用户选择**保项目隔离**。即：AWF MCP 按会话作用域挂载（每个项目各自的 `AWF_PROJECT_ROOT`）；worker 对 **MCP 工具**的约束采用身份/提示词协议（与 CC 现有做法同类，不视为新增降级）；对 **非 MCP 工具**（bash/read/write 等）DSH 原生 `tools.restrict()` 仍提供真实硬拦，继续使用。全局挂载 + 按调用指定项目的方案**不采用**。

**落地影响（同步进执行记录与 spec）**：T-P0-07（X2）按此收口；C17～C20 的 worker 权限实现形态以此为准；「子 Agent 身份与工具权限不降级」在 DSH 侧的解释被限定为「工具暴露面不削减 + 非 MCP 工具可硬拦 + MCP 工具靠 worker 协议」。


## 2026-09-18 P0 实测结论同步（X1～X9）

> 结论与证据以 [执行记录](dsh-adapter-execution.md) 为准；本节只做状态同步，不改动任何历史回答与选项。
> 验证基线：**dsh 0.1.5-rc.2**，隔离 `DSH_HOME=/tmp/awf-dsh-probe`（真实 `~/.dsh` 全程零改动）。

| 核查项 | 状态 | 一句话结论 |
|---|---|---|
| X1 会话级 MCP | **已验证** | 按会话挂 `AWF_PROJECT_ROOT` 可行；两项目实测不串（`0.2.0\|1` / `9.9.9\|2`） |
| X2 worker 权限 | **已验证（U15 收敛）** | `toolFilter` deny 真硬拦；但会话级 MCP 工具无法被 restrict 点名 → 用户选「保项目隔离」 |
| X3 决策拦截 | **护栏已验证** | `tools.guard()` 执行前拦截生效（被禁调用未到服务端）；回合末门阀按 U10 归 AWF |
| X4 cancel/队列/父子停止 | **已验证（reset 除外）** | 完成信号=`whenIdle()`；取消父会话**不停**子 Agent；reset 无 in-place 清空 |
| X5 单任务真实接线 | **已验证（continuable 除外）** | 真实落账闭环：模型经 MCP 工具改 state，磁盘回读 `pending→done` |
| X6 用量/UI 通路 | **部分** | 插件 HTTP 路由+cookie 可用；client 半侧在真实浏览器执行并注册 entry；设置页/业务页面待 P4 |
| X7 短任务隔离 | **已验证** | `ctx.llm.stream` 无状态调用；取消生效；**未创建任何会话** |
| X8 安装/技能/返回体 | **部分** | 技能发现属 agent preset 平面（当前取法看不到）；**返回体 346KB、`summary:true` 静默无效** |
| X9 原生输入/页面关闭 | **部分** | 关页面不影响后台运行；U14 交互待实机确认；F34 approval 阻塞需决策 |

**P0 期间新增的两个待决策项**（均已在执行记录留证据，未擅自处置）：
1. **F34**：子会话执行 `bash` 会停在 `approval/asked`，无人值守 run 会被人工批准卡死 → DSH 侧 approval 策略需定（受控自动批准 vs 接入 AWF 决策）。
2. **C15**：DSH 无 in-place 清空上下文能力 → 需要确认「新建会话承接已落盘交接」是否可作为 C15 的等价语义。


## 2026-09-18 P0 收尾：两项新增确认（U16 / U17）

> 来源：P0 实测（E-09 的 `approval/asked` 阻塞、E-06 的 reset 不可用）。回答由用户在本次会话中给出，**已确认**。

### U16 无人值守时子会话的工具批准怎么处理【已确认】【单选】

**实测**：web 组合默认 `workspace-write + approval: ask`，子会话执行 `bash` 会停在 `approval/asked`；页面无人点、宿主不代批 → 无人值守 run 被卡死（执行记录 F34）。

**回答记录（2026-09-18）**：选择**受控自动批准**——AWF 自己创建的执行会话，在项目范围内自动批准；项目外仍需人工。
保持无人值守可跑，又不放开全局。

**落地机制（已定位）**：`ctx.permissionPresets.set(session, 'workspace-write')` —— 该预设 = `sandbox: workspace-write` + `approval: never`，
按会话生效（`dsh-permission-presets/lib/index.js` 的 `set/apply`）。**待 P2 接线并实测消除 `approval/asked`。**

> **落地机制更正（2026-09-19，P2 前置源码核对）**：上面这条「预设 = workspace-write + approval never」**与 0.1.5-rc.2 源码不符**：
> 默认预设表里 `workspace-write` 的 approval 是 **`ask`**，只有 `danger-full-access` 才是 `never`
> （`dsh-permission-presets/lib/index.js:80-90`）；且 approval 策略只有 `ask` / `never` 两个取值，`never` 在应答者之前
> 短路为 `'rejected'` —— **`never` 是自动拒绝，不是自动批准**（`dsh-user-approval/lib/index.js:37,74,178`）。
> 因此原记录的接线**不会消除 F34 的卡死**，改用 `danger-full-access` 又会同时失去沙箱，与「项目外仍需人工」相悖。
>
> **用户已确认的语义不变**（受控自动批准：AWF 创建的会话在项目范围内自动批准、范围外仍需人工）；变的只是实现路径：
> 用平台原生的**批准应答者缝** —— 会话保持 `workspace-write` 预设，插件在 AWF 创建的 agent 作用域内注册
> `ctx.on('approval/request', …)`，范围内回 `'allowed-once'`、范围外 `next()` 委派给 UI/人工
> （`dsh-user-approval/lib/index.js:175-192`；应答者按 agent 作用域分发，取值见 `dsh-scope/lib/invariant.js:23`）。
> 详见执行记录 §2.5 B/C；**待 P2-5 真机实测确认消除 `approval/asked`**。

### U17 上下文清空（C15）的等价语义【已确认】【单选】

**实测**：DSH 0.1.5-rc.2 无 in-place 清空会话上下文的能力（`fork` 非清空、`compaction` 腾窗口但不删历史、`inbox` 只清排队）。

**回答记录（2026-09-18）**：接受「**换新会话 + 交接**」为等价语义——上一段对话不再影响后续任务、且交接已完成即视为达成；
实现为新建会话 + 注入 handoff 快照，旧会话保留可复盘。

**补充**：用户同时指出「有压缩可用」。核对结论见执行记录 F35——压缩确实存在（`compactNow` / `/compact`），
但位于 preset 的 isolate realm，root ctx 与 `agent.ctx` 都取不到；AWF 需在**与 preset 同 realm** 的作用域接线或直接驱动 `/compact`。
压缩作为「不换会话时延长可用轮次」的辅助手段保留，**不替代** C15。
