# plan-tasks 提示词

由 CLI 在 plan 阶段任务列表步骤调用 `claude -p` 时使用。

## 调用时机

plan 流程第 4 步：WBS 已生成，开始生成任务列表。

## 输入

- 已生成的 WBS 空间树
- 门禁任务定义
- 任务模板（来自 awf-plan-tasks 技能）

## 输出

- 任务列表（含优先级、依赖关系、验收标准）
- 每个任务的执行提示词（由 awf-plan-prompt 技能生成）
- 门禁任务在列表中的位置

## 约束

- 任务之间有明确的依赖链
- 每个任务有可独立验证的完成条件
- 门禁任务插入到关键节点
